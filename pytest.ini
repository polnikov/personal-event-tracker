[pytest]
testpaths = tests
# Coverage runs by default (pytest-cov). --cov-fail-under is a floor set just
# below the current ~78% so it gates against regressions without being
# aspirational; raise it as coverage grows.
addopts = -q --cov=app --cov-report=term-missing --cov-fail-under=75
