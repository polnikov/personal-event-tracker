/// <reference types="vite/client" />

// Injected by vite.config.ts `define` from package.json version. Used as
// the persistQueryClient buster so a fresh deploy invalidates cached state.
declare const __APP_VERSION__: string;
